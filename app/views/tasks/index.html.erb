<div class="container mt-5 d-flex flex-column align-items-center">
  <h1 class="mb-4">体を動かすことは安産につながります。</h1>
  <h1 class="mb-4">今日はどんな気分ですか？</h1>

  <div class="task-list w-50"> <!-- 幅50%にして中央寄せ -->
    <% @tasks.each do |task| %>
      <div class="task-item mb-3"> 
        <%= link_to task_path(task[:id]), class: "task-button d-block text-center p-3 rounded shadow-sm" do %>
          <%= task[:title] %>
        <% end %>
      </div>
    <% end %>
  </div>
</div>

<style>
  .task-button {
    background-color: #E6E6FA; /* 薄紫 */
    color: #000000;
    border: 1px solid #C8C8FF;
    text-decoration: none;
    font-weight: bold;
    font-size: 20px;
    width: 100%;  /* 親幅いっぱいに広げる */
    transition: background-color 0.2s;
  }

  .task-button:hover {
    background-color: #D8BFD8;
    color: #000000;
  }

  .task-item {
    margin-bottom: 1.5rem;
  }
</style>
